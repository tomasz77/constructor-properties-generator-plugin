# constructor-properties-generator-plugin

This is simple plugin generating @ConstructorProperty annotation with field names on at least one arg constructor.

Action visible in generate actions for class.
